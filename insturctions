first of all YOU MUST NOT change the compile ouput it must go from .rub to a runable binary with nothing inbeetween and must not just change to onother language

fix all of these

Strings that work — str type exists but you can't concatenate, get length, slice, compare, or do anything with strings besides print a literal.
break — no way to exit a loop early.
Error messages — syntax errors and type mismatches silently produce wrong output or crash the compiler with a Python traceback. No line numbers, no "undefined variable" errors.
mut is not enforced — let x and let mut x are treated identically. Writing to a non-mut variable isn't caught.
Class methods — classes can only hold data, no fn inside a class. You can't do stuff.greet().
None — no way to represent "no value", so you can't write functions that might fail.
Type inference gaps — let x = some_fn() doesn't know what type x is if the return type isn't annotated, so it defaults to i64 even if the function returns a float or string.
No else if chained properly — the parser handles it but it's fragile.
Floats with decimal literals — 3.14 crashes the lexer. You can only write whole-number floats like 3.
input() — can't read from the user.
file I/O — can't read or write files.
string functions — no len, split, contains, to_int.
type casting syntax — no x as i64 or int(x).
Multi-file compilation / real import — import is parsed but does nothing.
Real threads — currently sequential.

